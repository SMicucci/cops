#include <endian.h>
#define COPS_ASSERT_ENABLE
#include "../src/set.h"

#include "test.h"

init_slice(Entity, ent_slice);
init_set(Entity, ent_set, ent_slice);

init_slice(Ptr, slice);
init_set(Ptr, set, slice);

int main(void)
{
        ;
        return 0;
}
